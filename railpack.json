{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["python -m pip install -r requirements-railway.txt"]
    }
  },
  "deploy": {
    "startCommand": "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  }
}

